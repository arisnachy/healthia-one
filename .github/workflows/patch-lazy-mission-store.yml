name: HealthIA guarded lazy mission store patch

on:
  push:
    branches:
      - kira/architecture-100-20260821
    paths:
      - scripts/patch_lazy_mission_store.py
      - .github/workflows/patch-lazy-mission-store.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Apply exact-anchor patch
        run: python scripts/patch_lazy_mission_store.py
      - name: Verify scope and syntax
        run: |
          python -m py_compile healthia_one/google_mission_runtime.py
          git diff --check
          git diff -- healthia_one/google_mission_runtime.py
      - name: Commit patch
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "healthia-architecture-bot"
          git config user.email "actions@users.noreply.github.com"
          git add healthia_one/google_mission_runtime.py
          git commit -m "architecture: lazily construct mission Firestore store"
          git push origin HEAD:kira/architecture-100-20260821
