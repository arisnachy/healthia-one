name: Superseded fixed-revision browser proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-22.04
    steps:
      - name: Explain superseded proof
        run: |
          echo "This workflow intentionally no longer runs on pull requests."
          echo "Use candidate-cloud-browser-proof.yml so the browser proof is bound to the exact newly deployed candidate SHA instead of the historical healthia-one-judge-00005-xh8 revision."
