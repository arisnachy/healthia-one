name: KIRA retired FCM sender-project diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-22.04
    steps:
      - run: echo retired
