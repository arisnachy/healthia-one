if (!window.__HEALTHIA_RUNTIME_INTEGRATIONS__) {
  window.__HEALTHIA_RUNTIME_INTEGRATIONS__ = true;
(() => {
  const $=selector=>document.querySelector(selector), i18n=window.HealthIAI18n, text=(en,es)=>i18n?.locale==="es"?es:en, localeTag=()=>i18n?.browserLocaleTag?.()||"en-US";
  const APK_WORKFLOW_URL="https://github.com/arisnachy/healthia-one/actions/workflows/android-bridge.yml", ANDROID_GUIDE_URL="https://github.com/arisnachy/healthia-one/blob/main/docs/CONNECT_ANDROID.md", ANDROID_SOURCE_URL="https://github.com/arisnachy/healthia-one/tree/main/android-health-bridge";
  function toast(message){const node=$("#toast");if(!node)return;node.textContent=message;node.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>{node.hidden=true;},4200);}
  async function json(path,options={}){const headers=new Headers(options.headers||{});headers.set("Accept-Language",i18n?.locale||"en");const response=await fetch(path,{...options,headers});let payload={};try{payload=await response.json();}catch{}if(!response.ok)throw new Error(payload.detail||`Error ${response.status}`);return payload;}
  function setContinuityState(label,readiness){
    if(!label)return;
    const connected=readiness?.llm_backend==="gemini_api"&&readiness?.ai_ready===true;
    const limited=readiness?.llm_backend==="gemini_api"&&!readiness?.ai_ready;
    label.dataset.runtimeBackend=readiness?.llm_backend||"unknown";
    label.dataset.runtimeModel=readiness?.model||"";
    label.dataset.aiReady=String(Boolean(readiness?.ai_ready));
    label.dataset.adkReady=String(Boolean(readiness?.adk_ready));
    label.dataset.storeBackend=readiness?.store_backend||"";
    label.dataset.evidenceBackend=readiness?.evidence_backend||"";
    label.dataset.aiState=connected?"connected":limited?"limited":"local";
    label.textContent=connected?text("Continuity connected","Continuidad conectada"):limited?text("Continuity limited","Continuidad limitada"):text("Local continuity","Continuidad local");
    label.title=connected?text("HealthIA can use its configured intelligence when your request needs it.","HealthIA puede usar su inteligencia configurada cuando tu solicitud lo necesita."):limited?text("Some intelligent features are temporarily limited; your saved health context remains available.","Algunas funciones inteligentes están temporalmente limitadas; tu contexto de salud guardado sigue disponible."):text("HealthIA is running locally without external model requests.","HealthIA funciona localmente sin solicitudes externas al modelo.");
    label.classList.remove("runtime-ai-control");
    label.removeAttribute("role");
    label.removeAttribute("tabindex");
    label.removeAttribute("aria-label");
    label.onclick=null;
    label.onkeydown=null;
  }
  async function loadContinuityState(){const label=$("#runtimeLabel");if(!label)return;try{setContinuityState(label,await json("/api/readiness"));}catch(error){label.dataset.aiState="unknown";label.textContent=text("Continuity available","Continuidad disponible");label.title=text("HealthIA remains available; detailed runtime status could not be refreshed.","HealthIA sigue disponible; no se pudo actualizar el estado detallado del runtime.");}}
  function setupContinuityStatus(){loadContinuityState();}
  function setupRailReopen(){const shell=$("#app"),reopen=$("#expandLeft"),collapse=$("#collapseLeft");if(!shell||!reopen)return;reopen.textContent="›";const sync=()=>{const collapsed=shell.classList.contains("left-collapsed"),label=collapsed?text("Open sidebar","Abrir barra lateral"):text("Sidebar open","Barra lateral abierta");reopen.setAttribute("aria-expanded",String(!collapsed));reopen.title=label;reopen.setAttribute("aria-label",label);};reopen.onclick=()=>requestAnimationFrame(sync);collapse?.addEventListener("click",()=>requestAnimationFrame(sync));document.addEventListener("keydown",event=>{if(!(event.ctrlKey||event.metaKey)||event.key.toLowerCase()!=="b")return;event.preventDefault();shell.classList.toggle("left-collapsed");sync();});sync();}
  function setupVoiceInput(){const button=$("#voiceButton"),input=$("#chatInput");if(!button||!input)return;const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Recognition){button.title=text("Voice dictation is unavailable in this browser","El dictado no está disponible en este navegador");button.onclick=()=>toast(button.title);return;}const recognition=new Recognition();recognition.lang=localeTag();recognition.continuous=false;recognition.interimResults=true;let listening=false,prefix="";const setListening=active=>{listening=active;button.classList.toggle("is-listening",active);button.setAttribute("aria-pressed",String(active));const label=active?text("Stop dictation","Detener dictado"):text("Dictate message","Dictar mensaje");button.setAttribute("aria-label",label);button.title=label;};recognition.onstart=()=>{prefix=input.value.trim();recognition.lang=localeTag();setListening(true);};recognition.onresult=event=>{let transcript="";for(let index=0;index<event.results.length;index+=1)transcript+=event.results[index][0]?.transcript||"";input.value=[prefix,transcript.trim()].filter(Boolean).join(prefix?" ":"");input.dispatchEvent(new Event("input",{bubbles:true}));};recognition.onerror=event=>toast(event.error==="not-allowed"?text("Allow microphone access to use dictation.","Permite el micrófono para usar el dictado."):text(`Could not start dictation (${event.error||"error"}).`,`No pude iniciar el dictado (${event.error||"error"}).`));recognition.onend=()=>setListening(false);button.onclick=()=>{try{if(listening)recognition.stop();else recognition.start();}catch(error){toast(error.message||text("Could not start dictation.","No pude iniciar el dictado."));setListening(false);}};}
  function enhanceDeviceConnection(){const connect=$("#connectDevice");if(connect){connect.textContent=text("Connect phone or watch","Conectar teléfono o reloj");connect.title=text("Install the Android bridge, generate a pairing code, and authorize Health Connect","Instala el puente Android, genera un código y autoriza Health Connect");}const dialog=$("#deviceConnectDialog");if(!dialog||dialog.dataset.connectionEnhanced==="true")return Boolean(dialog);dialog.dataset.connectionEnhanced="true";const actions=dialog.querySelector(".pairing-actions");if(actions){const existing=actions.querySelector("a");if(existing){existing.href=APK_WORKFLOW_URL;existing.textContent=text("Download test APK ↗","Descargar APK de prueba ↗");}actions.insertAdjacentHTML("beforeend",`<a href="${ANDROID_GUIDE_URL}" target="_blank" rel="noreferrer">${text("Step-by-step guide ↗","Guía paso a paso ↗")}</a><a href="${ANDROID_SOURCE_URL}" target="_blank" rel="noreferrer">${text("Android source ↗","Código Android ↗")}</a>`);}const firstStep=dialog.querySelector(".device-connect-steps article:first-child p");if(firstStep)firstStep.textContent=text("Open Download test APK, choose the latest run, and download the HealthIA-Bridge-debug artifact.","Abre Descargar APK de prueba, entra en la ejecución más reciente y baja el artefacto HealthIA-Bridge-debug.");const panel=dialog.querySelector(".pairing-panel");if(panel&&!dialog.querySelector(".lan-help"))panel.insertAdjacentHTML("beforebegin",`<div class="lan-help"><strong>${text("Phone and computer on the same Wi-Fi","Teléfono y computadora en la misma Wi‑Fi")}</strong><span>${text("The phone cannot use 127.0.0.1. Use the LAN address printed by the launcher.","El teléfono no puede usar 127.0.0.1. Usa la dirección LAN que imprime el lanzador.")}</span></div>`);return true;}
  function setupDeviceConnection(){enhanceDeviceConnection();[120,350,800,1600].forEach(delay=>setTimeout(enhanceDeviceConnection,delay));document.addEventListener("click",event=>{const target=event.target.closest?.('[data-open="devices"], #connectDevice');if(target)requestAnimationFrame(()=>setTimeout(enhanceDeviceConnection,0));});}
  function init(){setupRailReopen();setupVoiceInput();setupContinuityStatus();setupDeviceConnection();}
  document.addEventListener("healthia:locale-changed",()=>{setupRailReopen();setupContinuityStatus();enhanceDeviceConnection();});
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();
}